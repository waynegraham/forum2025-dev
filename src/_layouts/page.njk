---
layout: default.njk
---

<!-- Hero Section -->
<header id="hero" class="h-96 bg-cover bg-center relative" >
    <div class="absolute inset-0 bg-black bg-opacity-50 flex items-center justify-center">
        {# <h1 class="text-white text-4xl md:text-6xl font-bold"> #}
        <h1 class="block font-sans text-4xl font-semibold !leading-snug tracking-normal text-white antialiased lg:text-5xl">
            {{ title }}
        </h1>
    </div>
</header>

<main class="container mx-auto flex flex-col items-center px-4 py-10">
    {{ content | safe }}
</main>